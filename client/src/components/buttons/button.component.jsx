import React from 'react';

const Button = ({isButton = true, title='', action, href, moreStyle}) => {
  const style = `font-bold rounded-md px-3 py-2 text-base cursor-pointer animate focus:outline-none`;
  return (
    <div>

    </div>
  );
};

export default Button;
